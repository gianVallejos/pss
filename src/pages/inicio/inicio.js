import React from 'react'

import './inicio.css'

function Inicio(props){
      return(
        <section className="Inicio">
          { props.children }
        </section>
      )
}

export default Inicio
